{{#each this}}

  <div class="flex">
    <div class="float-left">
      <p><strong>{{user.username}}: </strong>{{comment}}
        on
        {{format_date created_at}}</p>
    </div>
  </div>

{{/each}}